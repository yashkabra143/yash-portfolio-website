name: Daily SEO Maintenance

on:
  schedule:
    - cron: '30 3 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  seo-daily:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello"
